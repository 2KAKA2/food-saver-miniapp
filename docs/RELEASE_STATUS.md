# 发布状态与验证证据

更新日期：2026-08-17

## 已验证

- 微信登录采用小程序 code 到服务端 `code2session`，客户端不接触 AppSecret。
- 服务端会话仅保存令牌哈希，业务接口按登录用户和家庭成员关系鉴权。
- 多家庭创建、邀请、加入、切换、移除成员、退出和所有者权限已经具备测试覆盖。
- 小程序可修改家庭名称、撤销邀请码、转让家庭所有者和退出家庭；家庭页手机布局无横向溢出。
- 库存、菜谱和消耗记录按家庭隔离；跨家庭访问测试返回拒绝。
- 菜谱制作采用数据库事务和幂等键，重复请求不会重复扣减库存。
- AI 返回内容按不可信输入处理：限制条目数量和字段长度，并用真实库存覆盖模型提供的名称、单位和可用量；模型不能伪造库存或超量消耗。
- 图片识别结果逐项校验，非法数量和格式会被丢弃，最多返回 20 项且仍须用户确认后才会入库。
- 图片上传限制为 5 MB，仅接受 JPG、PNG、WebP，并校验内容签名与声明格式一致。
- 登录不读取微信昵称或头像；用户可自行设置家庭显示昵称。
- 登录时服务端校验并保存用户同意的协议版本和时间；协议更新后，旧版本会话自动失效并要求重新阅读和登录。
- 首发版本显式关闭 DCloud `uniStatistics`，不向第三方统计服务上报页面访问和活跃数据。
- 用户协议、隐私政策、退出登录和账号注销入口均已提供。
- 隐私政策的运营主体、协议日期和 AI 服务方由正式构建配置注入；占位值会被发布体检阻止。
- 已生成原创 1254 × 1254 PNG 小程序头像，核心图形位于安全区且不含文字、商标或水印。
- 原生底部导航已为五个页面配置 81 × 81 PNG 图标，并提供灰色默认态和绿色选中态。
- 生产配置会拒绝 SQLite、缺少微信/AI/Redis 配置、开发登录和演示数据。
- 生产环境不公开 Swagger、ReDoc 和 OpenAPI 文档；微信正式构建开启服务器域名校验。
- 数据库与 Redis 就绪检查、请求编号、脱敏访问日志和异常日志已实现。
- MySQL 备份生成 SHA-256 校验文件并默认轮转 30 天；恢复操作要求明确确认并自动创建恢复前备份；容器日志限制为 5 × 10 MB。

## 自动验证结果

- 后端：21 个 pytest 用例通过。
- 数据库：Alembic 无遗漏迁移。
- Python 生产依赖：`pip-audit` 未发现已知漏洞。
- 前端依赖：除 DCloud 当前强制使用的 Vite 5 构建工具例外外，无其他高危或严重漏洞；CI 会阻止新增问题。
- 构建：H5 和 `mp-weixin` 均成功。
- 部署：Docker Compose 配置解析通过。
- 发布配置：示例模板结构检查通过，含占位符的配置会被正式发布体检阻止。

## 手机端端到端验收

在 390 × 844 手机视口、本地开发登录、关闭真实 AI Key 的条件下完成：

1. 登录并进入空库存首页。
2. 首页显示三步首次使用引导，无横向溢出。
3. 通过快捷项录入 2 个西红柿。
4. AI 菜谱页读取到该库存并生成降级菜谱。
5. 确认制作并消耗 1 个西红柿。
6. 返回后库存显示为 1 个。
7. 菜谱历史显示对应菜谱状态为“已制作”。
8. 隐私政策显示运营主体、AI 服务方、保存期限和客服入口；“我的家庭”显示联系运营者入口，页面无横向溢出。

## 已确认的真实环境信息

- 微信小程序 AppID 已确认为 `wx30ddd1061d78b551`，并已写入 `manifest.json`。
- 注册主体类型为个人。
- 当前阶段使用本地电脑作为后端，已提供同一 Wi-Fi 下的真机联调配置脚本和操作文档。
- 模型预算尚未确定，因此继续保留真实 AI 与演示降级双通道。

## 尚待真实环境完成

- 在本机 `api/.env` 中配置同一 AppID，并由用户私下填写 AppSecret；禁止通过聊天或 Git 传递。
- 登录微信公众平台，根据个人主体当时可选范围确认服务类目和审核材料。
- 确认运营主体展示名称并在微信公众平台开通客服能力。
- 确认预算并购买云服务器、域名，完成实名认证和 ICP 备案。
- 配置 HTTPS、微信 request/uploadFile 合法域名和真实 AI Key。
- 在微信开发者工具中完成真机登录、家庭双账号共享和图片识别验证。
- 配置服务器外部的可用性监控与告警接收渠道。
- 上传体验版、完成微信隐私保护指引、提交审核并正式发布。

以上外部步骤完成前，项目不能被描述为已经正式上线。
